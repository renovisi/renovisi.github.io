---
const { 
    class: className = '',
    ...rest
} = Astro.props;
---

<button class={`bg-blue-500 hover:bg-blue-600  p-3 sm:p-4 rounded-full transition cursor-pointer ${className}`} {...rest}>
	<slot />
</button>