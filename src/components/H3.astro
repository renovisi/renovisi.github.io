---
const { 
    class: className = '', 
    ...rest
} = Astro.props;
---

<h3 class={`font-bold tracking-tight text-black text-2xl sm:text-3xl xl:text-4xl ${className}`} {...rest}>
    <slot />
</h3>