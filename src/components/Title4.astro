---
const { 
    class: className = '', 
    ...rest
} = Astro.props;
---

<p class={`font-bold tracking-tight text-black text-lg sm:text-xl xl:text-2xl prompt ${className}`} {...rest}>
    <slot />
</p>