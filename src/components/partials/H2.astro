---
const { 
    id: idName = '',
    class: className = '', 
} = Astro.props;
---

<h2 id={idName || undefined} class={`font-medium text-white text-3xl sm:text-4xl lg:text-6xl ${className}`}>
	<slot />
</h2>